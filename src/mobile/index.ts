export { useUserStore, DEMO_USERS } from '../shared/stores/userStore'
export { useServerStore, mockUsers } from '../shared/stores/serverStore'
export type { Server, Channel, User, Message } from '../shared/types'
export { socketService } from '../platform/api/socketService'
